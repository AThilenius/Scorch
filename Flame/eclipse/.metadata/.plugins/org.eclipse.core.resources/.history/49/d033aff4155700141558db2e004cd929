package com.thilenius.flame;

public class SparkRenderer implements TileEntitySpecialRenderer{
    
    public MyTESR(){
        // initialize anything that will be needed later by the renderer,
        // like textures and custom 3d models
    }

    @Override//<- if you don't know why you are writing this, google it NOW!
    public void renderTileEntityAt(TileEntity tileentity, double x, double y, double z, float f) {
        // here is where the rendering code will go
    }
}